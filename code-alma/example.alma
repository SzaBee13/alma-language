alma greet(name) {
    food("Welcome!");
    ate (name == "Flower") {
        food("Hi, " + name + "!");
    } else {
        food("I didn't know you!");
    }
    fin name;
}

food(greet("Flower"));

alma hiThere(name=null) {
    ate (name) {
        fin name
    }
}

food(hiThere("Alma"))