alma greet(name) {
    food("Welcome!");
    ate (name == "Flover") {
        food("Hi, " + name + "!");
    } else {
        food("I didn't know you!");
    }
    fin name;
}

food(greet("Flover"));
